package com.example.sigema.repositories;

import com.example.sigema.models.Grado;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IRepositoryGrado extends JpaRepository<Grado, Long> {
}
